# Original TAILER Idea

> Archived product note. Current checkpoint (2026-08-02): Iteration 1 persistence and Iteration 2 secure-provider implementation are complete; a disposable live OpenAI success smoke remains pending. See the [product charter](../product.md) and root [task board](../../tasks.md).

I have following idea:


I want to create a webapp which will split the API of LLMs and monitor the usage of such a split. The use-case is following. 

We are sitting in a hackathon right now. We are given an API for everyone. I want to create a platform which will create a subapis and monitor the usage of every user. It should also include easy configuration of the personal models users are using (global-individuall). The idea is to create indepented api key sharing which can be managed. The platform should include a Admin and User interface. 
